import styled from 'styled-components';
import { motion } from 'framer-motion';

export const Fixed = styled(motion.div)``;
export const Flex = styled(motion.div)``;

export const FlexCol = styled(motion.div)`
  display: flex;
  flex-direction: column;
`;

export const FlexWrap = styled(motion.div)``;
export const Relative = styled(motion.div)``;

export const MobileHiddenUl = styled(motion.ul)``;
export const MobileHidden = styled(motion.div)``;
export const LaptopHidden = styled(motion.div)``;
